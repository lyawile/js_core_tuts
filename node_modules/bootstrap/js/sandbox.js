let wanyamaWaPorini = [
  "monkey",
  "dog",
  "goat",
  "cow",
  "lioness",
  "zebra",
  "woof",
];
// let result = wanyamaWaPorini.fill(Math.random());
wanyamaWaPorini.fill(1);
// let valsNew = wanyamaWaPorini.map(test);
// function test(x) {
//   return Math.floor(x * 2 * Math.random() * 100);
// }
// console.log(valsNew);
let index = 0;
do {
  console.log(`The value is: ${index} `);
  index++;
} while (index < 0);

let vals = [12, 45, 1, 0, 10, 1555, 855555, 1252];
console.log(vals.sort());

let objects = [
  { x: 12, y: 10 },
  { x: 0, y: 5 },
];

let test = [10, 21, 3];

function compare(a, b) {
  return a.x - b.x;
}

let sorted = test.sort(compare);
console.log(test);

// callback function
const myFunc = (callback) => {
  console.log(callback);
};

function testFunction(name, age) {
  return `Your name is ${name} and age is ${age}`;
}
myFunc(testFunction("Hassan Lyawile", 34));

const ul = document.querySelector(".people");
let people = [
  "Hassan Lyawile",
  "Yusuf Hassan",
  "Yaaqub Hassan",
  "Twalib Amir",
  "Luqman Athman",
];
let html = "";
people.forEach((person) => {
  html += `<li style="color:purple; padding:10px">${person}</li>`;
});

// JavaScript Object
let user = {
  name: "Hassan Lyawile",
  username: "hassan.lyawile",
  address: "Zinga",
  login() {
    console.log(`user ${this.name} has logged in`);
  },
  logout() {
    console.log(`user ${this.name} has logged out`);
  },
};
console.log(html);
ul.innerHTML = html;

console.log(user.login());

// Fetch API
fetch("sunflower.webp")
  .then((response) => {
    return response.blob();
  })
  .then((response) => {
    console.log(response);
  })
  .catch((error) => {
    console.log("error: " + error);
  });

let list = ["hello", "good morning"];
let div = document.getElementById("app");
let data = "";
list.forEach((item) => {
  item = `<li style="color:red">${item}</li>`;
  data += item;
  div.innerHTML = data;
});

console.log(" The value of floor is: " + Math.floor(5.0));
console.log(" The value of ceil is: " + Math.ceil(5.0));

const elem = document.createElement("p");
elem.textContent  = 'I have been created on the fly';
const body = document.querySelector("body");
let testMath = Math.random();
console.log(testMath);
// body.appendChild(elem).innerHTML = ` Hello ${Math.ceil(testMath * 100)}`;
body.addEventListener("click", () => {
  body.appendChild(elem).innerHTML = testMath;
});

let myDiv = document.querySelector('.container');
let firstChild = myDi('p');
console.log("the first element " + firstChild);
myDiv.insertBefore(elem, firstChild);

console.log(myDiv);
